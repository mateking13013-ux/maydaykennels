---
// ContactSection component — Simplified two-column layout
---

<section class="contact" id="contact">
  <div class="contact-container">
    <div class="contact-info">
      <span class="contact-badge">Get In Touch</span>
      <h2 class="contact-title">Ready to Find Your Perfect Puppy?</h2>
      <p class="contact-desc">Have questions or ready to welcome a new furry family member? Fill out the form and we'll get back to you within 24 hours.</p>

      <div class="info-items">
        <div class="info-item">
          <div class="info-icon">
            <svg viewBox="0 0 24 24"><path d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 4l-8 5-8-5V6l8 5 8-5v2z"/></svg>
          </div>
          <div>
            <h4>Email Us</h4>
            <a href="mailto:info@maydaykennels.co.za">info@maydaykennels.co.za</a>
          </div>
        </div>

        <div class="info-item">
          <div class="info-icon">
            <svg viewBox="0 0 24 24"><path d="M11.99 2C6.47 2 2 6.48 2 12s4.47 10 9.99 10C17.52 22 22 17.52 22 12S17.52 2 11.99 2zM12 20c-4.42 0-8-3.58-8-8s3.58-8 8-8 8 3.58 8 8-3.58 8-8 8zm.5-13H11v6l5.25 3.15.75-1.23-4.5-2.67z"/></svg>
          </div>
          <div>
            <h4>Response Time</h4>
            <p>We typically respond within 24 hours</p>
          </div>
        </div>
      </div>

      <div class="location-note">
        <h4>Our Location</h4>
        <p>We operate as a family-based home breeder. For privacy and safety, we share our address after initial contact. We'll gladly arrange a visit.</p>
      </div>
    </div>

    <div class="contact-form-wrap">
      <form class="contact-form" id="homeContactForm">
        <input type="hidden" name="access_key" value="44364b8c-5dd7-4021-a1d8-d0965fad5373">
        <input type="hidden" name="subject" value="New Puppy Inquiry - Mayday Kennels">
        <input type="hidden" name="from_name" value="Mayday Kennels Website">

        <div class="form-row">
          <div class="form-group">
            <label for="homeFirstName">First Name</label>
            <input type="text" id="homeFirstName" name="firstName" required>
          </div>
          <div class="form-group">
            <label for="homeLastName">Last Name</label>
            <input type="text" id="homeLastName" name="lastName" required>
          </div>
        </div>

        <div class="form-row">
          <div class="form-group">
            <label for="homeEmail">Email</label>
            <input type="email" id="homeEmail" name="email" required>
          </div>
          <div class="form-group">
            <label for="homePhone">Phone</label>
            <input type="tel" id="homePhone" name="phone">
          </div>
        </div>

        <div class="form-group">
          <label for="homeMessage">Message</label>
          <textarea id="homeMessage" name="message" rows="4"></textarea>
        </div>

        <button type="submit" class="btn-submit">
          Send Message
          <svg viewBox="0 0 24 24"><path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/></svg>
        </button>
      </form>
    </div>
  </div>
</section>

<script>
  const form = document.getElementById('homeContactForm') as HTMLFormElement;

  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const formData = new FormData(form);
    const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement;
    const emailInput = form.querySelector('#homeEmail') as HTMLInputElement;
    formData.append("replyto", emailInput.value);
    const originalHTML = submitBtn.innerHTML;
    submitBtn.innerHTML = "Sending...";
    submitBtn.disabled = true;

    try {
      const response = await fetch("https://api.web3forms.com/submit", {
        method: "POST",
        body: formData
      });
      const data = await response.json();
      if (response.ok) {
        window.location.href = "/thank-you";
      } else {
        alert("Error: " + data.message);
      }
    } catch (error) {
      alert("Something went wrong. Please try again.");
    } finally {
      submitBtn.innerHTML = originalHTML;
      submitBtn.disabled = false;
    }
  });
</script>

<style>
  .contact {
    font-family: var(--font-family, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif);
    background: #ffffff;
    padding: 5rem 1.5rem;
  }

  .contact-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1.2fr;
    gap: 3rem;
    align-items: start;
  }

  .contact-badge {
    display: inline-block;
    background: rgba(46, 134, 171, 0.1);
    color: #2E86AB;
    padding: 0.4rem 1.25rem;
    border-radius: 50px;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 1.25rem;
  }

  .contact-title {
    font-size: 2.25rem;
    font-weight: 800;
    color: #1a2332;
    margin: 0 0 1rem;
    line-height: 1.2;
  }

  .contact-desc {
    font-size: 1rem;
    line-height: 1.7;
    color: #5a5a5a;
    margin: 0 0 2rem;
  }

  .info-items {
    display: flex;
    flex-direction: column;
    gap: 1.25rem;
    margin-bottom: 2rem;
  }

  .info-item {
    display: flex;
    align-items: flex-start;
    gap: 1rem;
  }

  .info-icon {
    width: 44px;
    height: 44px;
    background: #1a2332;
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .info-icon svg {
    width: 20px;
    height: 20px;
    fill: #2E86AB;
  }

  .info-item h4 {
    font-size: 0.95rem;
    font-weight: 600;
    color: #1a2332;
    margin: 0 0 0.2rem;
  }

  .info-item p,
  .info-item a {
    font-size: 0.9rem;
    color: #666;
    margin: 0;
    text-decoration: none;
  }

  .info-item a:hover {
    color: #2E86AB;
  }

  .location-note {
    background: #f8f9fa;
    border-radius: 12px;
    padding: 1.25rem;
    border-left: 3px solid #2E86AB;
  }

  .location-note h4 {
    font-size: 0.95rem;
    font-weight: 600;
    color: #1a2332;
    margin: 0 0 0.4rem;
  }

  .location-note p {
    font-size: 0.88rem;
    color: #666;
    line-height: 1.6;
    margin: 0;
  }

  .contact-form-wrap {
    background: #f8f9fa;
    border-radius: 20px;
    padding: 2.5rem;
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.4rem;
  }

  .form-group label {
    font-size: 0.88rem;
    font-weight: 600;
    color: #333;
  }

  .form-group input,
  .form-group textarea {
    padding: 0.8rem 1rem;
    border: 2px solid #e5e5e5;
    border-radius: 10px;
    font-size: 0.95rem;
    font-family: inherit;
    transition: all 0.3s ease;
    background: white;
  }

  .form-group input:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: #2E86AB;
    box-shadow: 0 0 0 3px rgba(46, 134, 171, 0.1);
  }

  .form-group textarea {
    min-height: 120px;
    resize: vertical;
  }

  .btn-submit {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: #2E86AB;
    color: white;
    padding: 1rem 2rem;
    border: none;
    border-radius: 50px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    margin-top: 0.5rem;
    box-shadow: 0 4px 15px rgba(46, 134, 171, 0.3);
  }

  .btn-submit:hover {
    background: #1B5E7B;
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(46, 134, 171, 0.4);
  }

  .btn-submit:disabled {
    opacity: 0.7;
    cursor: not-allowed;
  }

  .btn-submit svg {
    width: 18px;
    height: 18px;
    fill: currentColor;
  }

  @media (max-width: 900px) {
    .contact-container { grid-template-columns: 1fr; }
  }

  @media (max-width: 600px) {
    .contact { padding: 3rem 1rem; }
    .contact-title { font-size: 1.75rem; }
    .form-row { grid-template-columns: 1fr; }
    .contact-form-wrap { padding: 1.5rem; }
  }
</style>
